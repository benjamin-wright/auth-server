prefix: auth
domain: ponglehub.com.localhost

postgres:
  create: true
  deployment: "postgres"
  storage: "256Mi"

redis:
  create: true
  deployment: "redis"
  storage: "128Mi"

users:
  migrations:
    name: "migrations"
    # image:
    username: "usermigrations"
    secret: "user-migrations-pg"
  init:
    name: "init"
    secret: "user-init"
    # image:
    admin:
      username: "admin"
      # password: 
  # image:
  name: users
  replicas: 1
  postgres:
    database: "users"
    username: "users"
    secret: "users-pg"

tokens:
  # image:
  # keyfile:
  name: tokens
  replicas: 1

verify:
  # image:
  name: verify
  replicas: 1
  loginUrl: "http://ponglehub.com.localhost/auth/login"

forms:
  # image:
  urlPrefix: /auth
  name: forms
  replicas: 1
  # staticConfigMap: ""
  redis:
    unit: 0
    secret: "forms-redis"
