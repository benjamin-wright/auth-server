prefix: auth
domain: ponglehub.co.uk

postgres:
  create: false
  deployment: "postgres"
  storage: "256Mi"

redis:
  create: false
  deployment: "redis"
  storage: "128Mi"

users:
  migrations:
    name: "migrations"
    # image:
    username: "usermigrations"
    secret: "user-migrations-pg"
  # image:
  name: users
  replicas: 1
  postgres:
    database: "users"
    username: "users"
    secret: "users-pg"

tokens:
  # image:
  name: tokens
  replicas: 1

verify:
  # image:
  name: verify
  replicas: 1

forms:
  # image:
  urlPrefix: /auth
  name: forms
  replicas: 1
  # staticConfigMap: ""
  redis:
    unit: 0
    secret: "forms-redis"