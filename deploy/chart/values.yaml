prefix: auth

cockroach:
  create: false
  deployment: "cockroach"
  storage: "256Mi"

userService:
  # image:
  name: users
  replicas: 1
  cockroach:
    database: "users"
    username: "users"
    secret: "users-cockroach"

tokens: {}

verify: {}
